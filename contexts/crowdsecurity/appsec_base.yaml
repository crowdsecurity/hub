context:
  rules:
    - evt.Meta.rule_name
  target_uri:
    - evt.Meta.target_uri
  id:
   - match.id
  name:
   - match.name
  method:
   - match.method
  uri:
   - match.uri
  matched_zones:
   - match.matched_zones
  msg:
   - match.msg
