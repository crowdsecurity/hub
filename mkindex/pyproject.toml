[project]
name = "mkindex"
version = "0.1.0"
description = "Create index.json from the hub content"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jsonschema>=4.23.0",
    "pygithub>=2.6.1",
    "pyyaml>=6.0.2",
    "requests>=2.32.3",
]
