parsers:
- crowdsecurity/syslog-logs
- crowdsecurity/sshd-logs
- ./.tests/geoip-enrich/parsers/s02-enrich/crowdsecurity/geoip-enrich.yaml #we don't use the actual parser because we need to change the download URLs for the MMDBs
- crowdsecurity/dateparse-enrich

#manual parsers
scenarios:
- ""
postoverflows:
- ""
collections:
- ""
log_file: ssh-bf.log
log_type: syslog
# don't share the data directory with the other tests, we use a minimal GeoIP DB
own_data_dir: true
