type: trigger
name: crowdsecurity/segfault
description: "Trigger on segfault"
filter: "evt.Meta.log_type == 'kernel' && evt.Meta.sub_log_type == 'segfault'"
blackhole: 1m
labels:
  confidence: 1
  spoofable: 0
  classification:
    - attack.T1548.004
  behavior: "linux:exploitation"
  label: "Suspicious suid process crash"
  service: linux
  remediation: false
scope:
  type: exe
  expression: evt.Meta.exe
