type: trigger
name: crowdsecurity/http-open-proxy
description: "Detect scan for open proxy"
filter: "evt.Meta.log_type == 'http_access-log' && evt.Meta.http_status == '400' && evt.Parsed.verb == 'CONNECT'"
blackhole: 2m
labels:
 service: http
 type: scan
 remediation: true
