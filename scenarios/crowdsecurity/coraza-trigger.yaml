# type: trigger
# filter: evt.Parsed.program == 'coraza-waf' && evt.Parsed.action == "deny"
# debug: true
# name: crowdsecurity/coraza-trigger
# description: here we go
# blackhole: 2m
# labels:
#   type: exploit
#   remediation: true
# groupby: evt.Meta.source_ip
