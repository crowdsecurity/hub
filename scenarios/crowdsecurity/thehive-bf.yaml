type: leaky
debug: false
name: crowdsecurity/thehive-bf
description: "Detect bruteforce on Thehive web interface"
filter: "evt.Meta.service == 'thehive' && evt.Meta.auth_status == 'failed'"
leakspeed: "10s"
capacity: 5
groupby: evt.Meta.source_ip
blackhole: 5m
labels:
  service: thehive
  confidence: 3
  spoofable: 0
  classification:
    - attack.T1110
  behavior: "http:bruteforce"
  label: "The Hive Bruteforce"
  remediation: true
