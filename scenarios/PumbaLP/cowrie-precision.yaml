type: leaky
name: PumbaLP/cowrie-precision
description: "Aggressive precision detection for Cowrie Honeypot (SSH/Telnet)"
filter: "evt.Meta.log_type startsWith 'cowrie.login' || evt.Meta.log_type startsWith 'cowrie.command'"
groupby: "evt.Meta.source_ip"
capacity: 2
leakspeed: 5m
labels:
  service: cowrie
  type: bruteforce
  classification: honeypot
  author: PumbaLP
  remediation: true
