type: trigger
name: sigma/proc_creation_win_schtasks_schedule_type_system
description: |
  Detects scheduled task creations or modification to be run with high privileges on a suspicious schedule type
filter: |
  (evt.Meta.service == 'sysmon' && evt.Parsed.EventID == '1') && ((evt.Parsed.Image endsWith '\\schtasks.exe' || evt.Parsed.OriginalFileName == 'schtasks.exe') && (evt.Parsed.CommandLine contains ' ONLOGON ' || evt.Parsed.CommandLine contains ' ONSTART ' || evt.Parsed.CommandLine contains ' ONCE ' || evt.Parsed.CommandLine contains ' ONIDLE ') && (evt.Parsed.CommandLine contains 'NT AUT' || evt.Parsed.CommandLine contains ' SYSTEM' || evt.Parsed.CommandLine contains 'HIGHEST'))
blackhole: 2m
#status: experimental
labels:
  service: windows
  confidence: 0
  spoofable: 0
#  classification:
#  - attack.t1053.005

  label: "Suspicious Schtasks Schedule Type With High Privileges"
  behavior : "windows:audit"
  remediation: false

scope:
  type: ParentProcessId
  expression: evt.Parsed.ParentProcessId

