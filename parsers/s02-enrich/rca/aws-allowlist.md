# AWS Services allowlist

Allow and ignore IPs from AWS global services such as Cloudfront 
* https://ip-ranges.amazonaws.com/ip-ranges.json

It will whitelist overflows triggered on an IP in those lists