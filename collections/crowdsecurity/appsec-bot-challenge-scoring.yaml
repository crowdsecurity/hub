name: crowdsecurity/appsec-bot-challenge-scoring
# Bot Challenge scoring engine. Weights fingerprint signals into a request score.
# Pair it with a threshold config that decides when to reject
appsec-configs:
  - crowdsecurity/appsec-bot-challenge-scoring
description: "AppSec bot challenge: weighted fingerprint scoring engine (serves the challenge, computes the score)"
labels:
  label: "WAF - Bot Challenge Scoring"
author: crowdsecurity
tags:
  - waf
  - appsec
  - http
  - bot
