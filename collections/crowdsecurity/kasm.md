### KASM workspaces parser

This collection add parser support for kasm json log output, also a base scenario to detect bruteforce login attempts.

```yaml
filenames:
  - /opt/kasm/current/log/api_server_json.log
labels:
  type: kasm
```