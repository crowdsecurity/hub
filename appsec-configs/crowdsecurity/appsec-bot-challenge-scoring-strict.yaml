name: crowdsecurity/appsec-bot-challenge-scoring-strict
inband:
  on_challenge_submit:
    - filter: RequestScore() >= 45
      apply:
        - 'RejectSubmission("request score " + string(RequestScore()) + ": " + join(RequestScoreReasons(), ","), "verbose")'
